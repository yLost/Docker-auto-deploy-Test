name: CI

on:
  push:
    branches: [ main ]

jobs:
    build:
        name: Build
        runs-on: ubuntu-latest
        steps:
        - name: executing remote ssh commands using password
          uses: appleboy/ssh-action@master
          with:
            host: ${{ ssh.HOST }}
            username: ${{ ssh.USER }}
            password: ${{ ssh.PASSOWRD }}
            port: ${{ ssh.PORT }}
            script: |
              mkdir batata
